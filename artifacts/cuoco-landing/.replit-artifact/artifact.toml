kind = "web"
previewPath = "/"
title = "Cuoco Landing Page"
version = "1.0.0"
id = "artifacts/cuoco-landing"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18441

[services.development]
run = "pnpm --filter @workspace/cuoco-landing run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cuoco-landing", "run", "build" ]
publicDir = "artifacts/cuoco-landing/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18441"
BASE_PATH = "/"
